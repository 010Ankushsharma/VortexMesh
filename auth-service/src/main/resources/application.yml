server:
  port: 8770

spring:
  application:
    name: vortex-auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/vortexmesh_auth
    username: vortex
    password: vortex123
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
  security:
    filter:
      order: 10
