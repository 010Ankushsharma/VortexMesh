server:
  port: 8888

spring:
  application:
    name: vortex-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vortexmesh/config-repo
          default-label: main
          search-paths: '{application}'
